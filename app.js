const screens=[...document.querySelectorAll('.screen')];const tabs=[...document.querySelectorAll('.mobile-tabs button')];function showScreen(id){screens.forEach(s=>s.classList.toggle('active-screen',s.id===id));tabs.forEach(t=>t.classList.toggle('active',t.dataset.target===id));window.scrollTo({top:0,behavior:'instant'});}tabs.forEach(tab=>tab.addEventListener('click',()=>showScreen(tab.dataset.target)));function syncMode(){if(innerWidth<=700&&!document.querySelector('.screen.active-screen'))showScreen('inicio');}syncMode();addEventListener('resize',syncMode);document.addEventListener('DOMContentLoaded',()=>{if(window.gsap&&window.ScrollTrigger){gsap.registerPlugin(ScrollTrigger);gsap.from('.hero-copy>*',{opacity:0,y:24,duration:.7,stagger:.08,ease:'power2.out'});gsap.from('.hero-card',{opacity:0,x:35,duration:.9,ease:'power2.out'});document.querySelectorAll('.skills-grid article,.project-card,.project-links a,.identity-card,.xyz-card,.stats-grid article').forEach((el,i)=>{gsap.from(el,{scrollTrigger:{trigger:el,start:'top 88%',toggleActions:'play none none none'},opacity:0,y:30,duration:.6,delay:(i%3)*.05,ease:'power2.out'});});}const statSection=document.querySelector('#estadisticas');let counted=false;const runCounters=()=>{if(counted)return;counted=true;document.querySelectorAll('[data-count]').forEach(el=>{const target=Number(el.dataset.count);const obj={value:0};if(window.gsap){gsap.to(obj,{value:target,duration:1.5,ease:'power2.out',onUpdate:()=>el.textContent=Math.floor(obj.value).toLocaleString('es-CL')+'+'});}else el.textContent=target.toLocaleString('es-CL')+'+';});};new IntersectionObserver(entries=>{if(entries.some(e=>e.isIntersecting))runCounters();},{threshold:.3}).observe(statSection);});